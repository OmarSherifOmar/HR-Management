'use client';

import { useAuth } from '../../../context/AuthContext';
import { useRouter } from 'next/navigation';
import { useEffect, useState } from 'react';
import { Calculator, PlayCircle, Calendar, Users, TrendingUp, AlertCircle, CheckCircle } from 'lucide-react';
import DashboardLayout from '../../../components/DashboardLayout';
// Entitlements admin page removed — entitlements are handled at policy level.

export default function RemovedEntitlementsPage() {
  return null;
}
 
