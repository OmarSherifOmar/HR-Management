export enum OffboardingStatus {
  INITIATED = 'initiated',
  CLEARANCE_PENDING = 'clearance_pending',
  AWAITING_SIGNOFFS = 'awaiting_signoffs',
  ACCESS_REVOKED = 'access_revoked',
  SETTLEMENT_TRIGGERED = 'settlement_triggered',
  COMPLETED = 'completed',
  CANCELLED = 'cancelled',
}
